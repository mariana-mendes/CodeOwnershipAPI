const project = {
    name: "projeto-codeownership",
    authors: "Pessoa1, Pessoa2, Pessoa3",
    metric: {
            id: {
                type: mongoose.Schema.Types.ObjectId,
                ref: "Metric"
              }
            }
        }    